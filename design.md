# خطة تصميم واجهة «إدارة متجر»

## التوجه العام

يُصمَّم التطبيق للاستخدام اليومي السريع داخل محل صغير أو متوسط، وباتجاه **من اليمين إلى اليسار** وبوضع عمودي بنسبة **9:16**. تركز الواجهة على مسار البيع خلال ثوانٍ: مسح المنتج، التأكد من بياناته، إضافته إلى السلة، ثم إتمام البيع. تستلهم بنية الصفحات ومسافات اللمس الواضحة من إرشادات تطبيقات الهواتف الحديثة، مع لغة بصرية عملية متوافقة مع Material 3.

## قائمة الشاشات

| الشاشة | المحتوى الرئيسي | الوظائف |
|---|---|---|
| الرئيسية | ملخص المنتجات، تنبيهات المخزون، قيمة المخزون، مبيعات اليوم، اختصارات | الانتقال السريع لإضافة منتج أو بدء مسح للبيع |
| البيع | زر مسح باركود، سلة بيع، عناصر السلة، إجمالي الفاتورة | المسح، تعديل كميات السلة، حذف عنصر، إتمام البيع |
| ماسح الباركود | معاينة كاميرا كاملة الشاشة وإطار توجيه | قراءة EAN وUPC وCode 128 وCode 39 وQR، وإرجاع المنتج أو اقتراح إضافته |
| المنتجات | بحث فوري، تصنيفات، قائمة افتراضية عالية الأداء | البحث بالاسم أو الباركود، فتح التفاصيل، الإضافة |
| إضافة/تعديل منتج | الاسم، الصورة، السعران، الكمية، التصنيف، الباركود والوصف | اختيار/التقاط صورة، مسح باركود، تحقق من التكرار، الحفظ أو التعديل |
| تفاصيل المنتج | الصورة والمعلومات والمخزون وسجل مختصر | تعديل البيانات أو حذف المنتج بعد تأكيد |
| المخزون | إجماليات الكميات، النافد والمنخفض، قائمة قابلة للتصفية | تعديل كمية المنتج مباشرة |
| الإعدادات | اسم المحل، العملة، حد المخزون المنخفض، النمط، النسخ والاستعادة | حفظ تفضيلات المحل وإدارة النسخ المحلية |

## تدفقات الاستخدام الأساسية

| التدفق | الخطوات |
|---|---|
| بيع سريع | الرئيسية أو البيع ← مسح باركود ← عرض المنتج ← ضبط الكمية ← إضافة للسلة ← إتمام البيع ← خصم الكمية وتسجيل البيع |
| منتج غير مسجل | مسح باركود ← رسالة «هذا المنتج غير مسجل» ← إضافة منتج ← تعبئة المعلومات ← حفظ ← العودة للبيع أو المنتجات |
| إدخال منتج | المنتجات ← إضافة منتج ← اختيار صورة أو التقاطها ← مسح/إدخال باركود ← إدخال الاسم والسعر والكمية ← التحقق ← حفظ |
| ضبط المخزون | المخزون ← اختيار منتج ← إدخال الكمية الجديدة أو التعديل النسبي ← حفظ ← تحديث المؤشرات في الرئيسية |
| استعادة البيانات | الإعدادات ← إنشاء نسخة احتياطية أو اختيار ملف استعادة ← تأكيد ← استبدال البيانات المحلية بعد التحقق |

## التفاعل والتخطيط

تستخدم الشاشة السفلية خمسة تبويبات: **الرئيسية، البيع، المنتجات، المخزون، الإعدادات**. يظل زر «مسح باركود» أكبر إجراء مرئي في تبويب البيع، بينما تُعرض السلة في منطقة ثابتة في أسفل الشاشة لتقليل الحركة أثناء العمل بيد واحدة. يجب ألا يقل ارتفاع مناطق اللمس عن 48 نقطة، وتستخدم البطاقات زوايا دائرية معتدلة ومسافات داخلية مريحة، مع نصوص عربية واضحة ومحاذاة يمينية.

## الألوان والهوية

| العنصر | اللون الفاتح | اللون الداكن | الدور |
|---|---:|---:|---|
| اللون الأساسي | `#146C5A` | `#4FC7A8` | إجراءات البيع والمسح والحالة النشطة |
| الخلفية | `#F7FAF8` | `#101917` | خلفية هادئة تقلل إجهاد العين |
| السطح | `#FFFFFF` | `#192522` | البطاقات والحقول والقوائم |
| النص الأساسي | `#16201D` | `#E7F2EE` | العناوين والقيم |
| النجاح | `#1F8A5B` | `#5EE0A0` | إتمام البيع وتوفر المخزون |
| التحذير | `#B7791F` | `#F1B85A` | مخزون منخفض |
| الخطأ | `#C43D4B` | `#FF8A96` | عنصر نافد أو تحقق فاشل |

## نموذج البيانات المحلي

تُحفظ البيانات على الجهاز فقط في الإصدار الأول. يتضمن النموذج `Product` حقول المعرف والاسم والباركود الفريد والصورة وسعر البيع وسعر الشراء الاختياري والكمية ومعرف التصنيف والوصف وتاريخ الإنشاء والتحديث. يشمل `Category` اسم التصنيف ولونه، ويحتوي `Sale` على المبلغ الإجمالي والتاريخ، بينما يسجل `SaleItem` المنتج والكمية وسعر الوحدة وقت البيع. تحفظ الإعدادات المحلية اسم المحل والعملة وحد المخزون المنخفض والنمط.

> تعتمد النسخة الأولى مخزنًا محليًا دائمًا منظّمًا وقابلًا للترحيل لاحقًا إلى SQLite/Room عند إضافة التقارير المتقدمة أو المزامنة، دون تغيير منطق واجهة المستخدم.

